import { Card, Button, Pill } from "@/components/ui"; import { Play, Mic2, Sparkles } from "lucide-react";
export default function Voice(){return <Page title="Voice Studio"><Card><div className="grid lg:grid-cols-2 gap-5"><div><Pill>VOICE LIBRARY</Pill><h2 className="mt-3 text-xl font-bold">Choose your narrator</h2><div className="mt-5 space-y-2">{["Pria Dewasa — Tenang","Pria Dewasa — Tegas","Pria Dewasa — Storytelling","Wanita Dewasa — Natural"].map((x,i)=><button key={x} className={`w-full text-left p-4 rounded-2xl border ${i===0?"border-violet-400 bg-violet-500/10":"border-white/10 bg-white/[.03]"}`}><div className="flex items-center gap-3"><Mic2 size={18}/><span>{x}</span><Play size={15} className="ml-auto"/></div></button>)}</div></div><div className="rounded-3xl bg-black/20 border border-white/10 p-6 flex flex-col justify-center"><div className="text-sm text-zinc-500">Preview</div><div className="mt-6 h-24 flex items-center gap-1">{Array.from({length:45}).map((_,i)=><div key={i} className="flex-1 rounded-full bg-violet-400/50" style={{height:`${15+(i*17)%70}%`}}/>)}</div><Button primary className="mt-6"><Sparkles size={16} className="mr-2"/> Generate Voice</Button></div></div></Card></Page>}
function Page({title,children}:{title:string,children:React.ReactNode}){return <div className="space-y-5"><Pill>VOICE STUDIO</Pill><h1 className="text-4xl font-black">{title}</h1>{children}</div>}